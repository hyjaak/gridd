# Do not set script-shell to a Windows path here — it breaks `npm install` on Linux (e.g. Vercel).
# If PowerShell blocks lifecycle scripts on your machine, run once locally:
#   npm config set script-shell "C:\\Windows\\System32\\cmd.exe"
