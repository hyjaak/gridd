/**
 * In-app Uber OAuth + “request on Uber network” UI.
 * Set to `true` when Uber production API access is approved.
 * Server routes and price estimates stay enabled regardless.
 */
export const UBER_BOOKING_ENABLED = false;
