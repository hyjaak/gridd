"use client";

import { BitesMain } from "@/components/bites/BitesMain";

/**
 * Full-screen Bites (Gen Z). Bottom nav is hidden for `/bites` routes via CustomerNav.
 */
export default function BitesPage() {
  return <BitesMain />;
}
